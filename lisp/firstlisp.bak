#lang racket
(define double
  (lambda (x) (+ x x)))
(define (double2 x)
  (* x 2))
(define (factorial n)
  (if (= n 0)
      1
      (* n (factorial (- n 1)))))
((lambda (f)(f 5)) (lambda (x) (+ x 5)))
(struct node (key val left right))
;(define (dfs key root) ())
(define tree
  (list (list 1 2 3 4) (